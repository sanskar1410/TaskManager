# Local dev default — points at your Spring Boot backend running on localhost.
# For production (Vercel), set VITE_API_BASE_URL as an environment variable
# in the Vercel dashboard, pointing to your deployed Railway backend + /api.
VITE_API_BASE_URL=http://localhost:8080/api
